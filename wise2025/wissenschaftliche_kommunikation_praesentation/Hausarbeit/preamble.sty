% Sprachpaket und Worttrennung
\usepackage[ngerman]{babel}

% Zeichensatz
\usepackage[utf8]{inputenc}

% besserer Anhang
\usepackage{appendix}

% Paket für Bilder
\usepackage{graphicx}

% Paket zur Erstellung von Gantt-Charts
\usepackage{pgfgantt}

% Schönere ausgefranste Ränder
\usepackage[document]{ragged2e}

% für die darstellung von Quellcode
\usepackage{listings}

% Für Tabellen mit variabler Breite (wichtig für KI Verzeichnis)
\usepackage{tabularx}

% pdf-Einstellungen
\usepackage[
    pdftitle={Exposé GBI Tauri vs Electron},
    pdfsubject={Exposé},
    pdfauthor={Luca Michael Schmidt},
    pdfkeywords={Tauri, Electron, Rust, GBI},
    hidelinks % Links nicht einrahmen
]{hyperref}
\usepackage[a-1b]{pdfx}

% Kopf- und Fußzeilen
\usepackage{fancyhdr}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% ~~~~~ Dokumenteinstellungen ~~~~~
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Seitenränder - WICHTIG: Laut Anforderung "ALLE Seitenränder 25mm"
\usepackage[top=25mm, bottom=25mm, left=25mm, right=25mm, headsep=10mm, footskip=12mm]{geometry}

% Typographie
\usepackage[auto]{microtype}
\usepackage{tabulary}
\usepackage{array}
\urlstyle{same}

% Zeilenabstand (1.5 Zeilig ist Standard für Arbeiten)
\usepackage{setspace}
\onehalfspacing

% Nummerierungstiefe
\setcounter{secnumdepth}{3}
\setcounter{tocdepth}{3}

% Abstand zwischen Text und Fußnote
\setlength{\skip\footins}{8mm}

% Paragapheneinzug
\setlength{\parskip}{2mm}
\setlength{\parindent}{0pt}

% entfernt gezwunge Leerseiten
\let\cleardoublepage\clearpage

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% ~~~~~ Literaturverzeichnis ~~~~~
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\usepackage[
    backend=biber,
    style=ieee,
]{biblatex}
\bibliography{bib/references.bib}

% Behandlung langer URLs im Verzeichnis
\setcounter{biburlnumpenalty}{9000}
\setcounter{biburlucpenalty}{9000}
\setcounter{biburllcpenalty}{9000}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% ~~~~~ Bilddarstellung ~~~~~
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\usepackage[font=small, labelfont=bf, margin=0.3cm]{caption}
\usepackage{subcaption}

\newcommand{\source}[1]{\vspace{-4pt}\caption*{\footnotesize{Quelle: {#1}}}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% ~~~~~ Akronyme und Glossar ~~~~~
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\usepackage[acronym,xindy,toc]{glossaries}
\makeglossaries
%\loadglsentries{appendix/acronyms.tex}
%\loadglsentries{appendix/glossar.tex}

\renewcommand*{\glstextformat}[1]{\textit{#1}}
